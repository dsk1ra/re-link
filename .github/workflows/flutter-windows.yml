name: Flutter Windows CI

on:
  push:
    branches: [main, develop]
    paths:
      - 'client/flutter/**'
      - 'client/rust/**'
      - 'server/rust/**'
      - 'shared/rust/**'
      - '.github/workflows/flutter-windows.yml'
  pull_request:
    branches: [main, develop]
    paths:
      - 'client/flutter/**'
      - 'client/rust/**'
      - 'server/rust/**'
      - 'shared/rust/**'
      - '.github/workflows/flutter-windows.yml'
  workflow_dispatch:

permissions:
  contents: read

# audiopus_sys builds vendored opus through cmake, and its CMakeLists still
# declares cmake_minimum_required < 3.5, which CMake 4 rejects outright.
env:
  CMAKE_POLICY_VERSION_MINIMUM: "3.5"

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  flutter-quality:
    if: ${{ github.actor != 'nektos/act' }}
    name: Flutter Quality Checks
    runs-on: windows-latest
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable long file paths (Windows)
        run: git config --system core.longpaths true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
          architecture: x64

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo artifacts
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            client/rust/target
            server/rust/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Dart pub cache
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('client/flutter/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Enable Windows desktop
        run: flutter config --no-analytics --enable-windows-desktop

      - name: Pre-cache Windows artifacts
        run: flutter precache --windows

      - name: Cache cargo-installed codegen tools
        id: frb-tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/flutter_rust_bridge_codegen
            ~/.cargo/bin/flutter_rust_bridge_codegen.exe
            ~/.cargo/bin/cargo-expand
            ~/.cargo/bin/cargo-expand.exe
          key: ${{ runner.os }}-frb-tools-2.12.0

      - name: Install flutter_rust_bridge_codegen
        if: steps.frb-tools.outputs.cache-hit != 'true'
        run: |
          cargo install flutter_rust_bridge_codegen --version 2.12.0 --locked
          cargo install cargo-expand --locked

      - name: Add Cargo bin to PATH
        run: |
          "${env:USERPROFILE}\.cargo\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Ensure rust_builder exists
        working-directory: client/flutter
        run: |
          if (-not (Test-Path "rust_builder/pubspec.yaml")) {
            flutter_rust_bridge_codegen integrate --template app
          }

      - name: Generate rust_builder (Cargokit)
        working-directory: client/flutter
        run: flutter_rust_bridge_codegen generate --config-file flutter_rust_bridge.yaml

      - name: Fetch Dart/Flutter dependencies
        working-directory: client/flutter
        run: flutter pub get

      - name: Enforce Dart formatting
        working-directory: client/flutter
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze Flutter project
        working-directory: client/flutter
        run: flutter analyze

      - name: Run Flutter tests
        working-directory: client/flutter
        run: flutter test

  flutter-quality-act:
    if: ${{ github.actor == 'nektos/act' }}
    name: Flutter Quality Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
          architecture: x64

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo artifacts
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            client/rust/target
            server/rust/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Dart pub cache
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('client/flutter/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Cache cargo-installed codegen tools
        id: frb-tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/flutter_rust_bridge_codegen
            ~/.cargo/bin/flutter_rust_bridge_codegen.exe
            ~/.cargo/bin/cargo-expand
            ~/.cargo/bin/cargo-expand.exe
          key: ${{ runner.os }}-frb-tools-2.12.0

      - name: Install flutter_rust_bridge_codegen
        if: steps.frb-tools.outputs.cache-hit != 'true'
        run: |
          cargo install flutter_rust_bridge_codegen --version 2.12.0 --locked
          cargo install cargo-expand --locked

      - name: Add Cargo bin to PATH
        run: echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"

      - name: Ensure rust_builder exists
        working-directory: client/flutter
        run: |
          if [ ! -f rust_builder/pubspec.yaml ]; then
            flutter_rust_bridge_codegen integrate --template app
          fi

      - name: Generate rust_builder (Cargokit)
        working-directory: client/flutter
        run: flutter_rust_bridge_codegen generate --config-file flutter_rust_bridge.yaml

      - name: Fetch Dart/Flutter dependencies
        working-directory: client/flutter
        run: flutter pub get

      - name: Enforce Dart formatting
        working-directory: client/flutter
        run: dart format --output=none --set-exit-if-changed .

      - name: Analyze Flutter project
        working-directory: client/flutter
        run: flutter analyze

      - name: Run Flutter tests
        working-directory: client/flutter
        run: flutter test

  build-windows-release:
    if: ${{ github.actor != 'nektos/act' && (github.event_name == 'workflow_dispatch' || github.ref == 'refs/heads/main') }}
    name: Build Windows Release
    runs-on: windows-latest
    needs: flutter-quality
    defaults:
      run:
        shell: pwsh

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Enable long file paths (Windows)
        run: git config --system core.longpaths true

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
          architecture: x64

      - name: Set up Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo artifacts
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            client/rust/target
            server/rust/target
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Cache Dart pub cache
        uses: actions/cache@v4
        with:
          path: ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('client/flutter/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Enable Windows desktop
        run: flutter config --no-analytics --enable-windows-desktop

      - name: Pre-cache Windows artifacts
        run: flutter precache --windows

      - name: Cache cargo-installed codegen tools
        id: frb-tools
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/flutter_rust_bridge_codegen
            ~/.cargo/bin/flutter_rust_bridge_codegen.exe
            ~/.cargo/bin/cargo-expand
            ~/.cargo/bin/cargo-expand.exe
          key: ${{ runner.os }}-frb-tools-2.12.0

      - name: Install flutter_rust_bridge_codegen
        if: steps.frb-tools.outputs.cache-hit != 'true'
        run: |
          cargo install flutter_rust_bridge_codegen --version 2.12.0 --locked
          cargo install cargo-expand --locked

      - name: Add Cargo bin to PATH
        run: |
          "${env:USERPROFILE}\.cargo\bin" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append

      - name: Ensure rust_builder exists
        working-directory: client/flutter
        run: |
          if (-not (Test-Path "rust_builder/pubspec.yaml")) {
            flutter_rust_bridge_codegen integrate --template app
          }

      - name: Generate rust_builder (Cargokit)
        working-directory: client/flutter
        run: flutter_rust_bridge_codegen generate --config-file flutter_rust_bridge.yaml

      - name: Fetch Dart/Flutter dependencies
        working-directory: client/flutter
        run: flutter pub get

      - name: Build Windows app (Release)
        working-directory: client/flutter
        env:
          RUST_BACKTRACE: 1
        run: flutter build windows --release

      - name: Archive Windows build output
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: windows-build
          path: client/flutter/build/windows/x64/runner/Release/
          if-no-files-found: warn